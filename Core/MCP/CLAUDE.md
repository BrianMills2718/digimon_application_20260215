# Core/MCP

This subtree contains the MCP-facing runtime bridge for DIGIMON.

## Working Rules

- Keep MCP surfaces aligned with the underlying operator and graph contracts.
- Tool-calling ergonomics must not silently distort runtime semantics.
