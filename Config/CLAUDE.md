# Config

This subtree contains legacy capital-C configuration modules and related Python
config surfaces used by DIGIMON.

## Working Rules

- Keep config semantics explicit and fail loud on missing required values.
- Preserve alignment with the active lowercase `config/` and runtime docs when
  these settings overlap.
