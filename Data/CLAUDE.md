# Data

This subtree contains datasets and benchmark corpora used by DIGIMON.

## Working Rules

- Treat dataset directories as mostly read-only inputs.
- Do not add local instruction files to every dataset folder unless a dataset
  has its own active operational workflow.
