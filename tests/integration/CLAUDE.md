# tests/integration

This subtree contains integration tests across DIGIMON components.

## Working Rules

- Use this layer for real component wiring, not mocked facsimiles of the
  operator stack.
- Keep assumptions about models, corpora, and graph state explicit.
