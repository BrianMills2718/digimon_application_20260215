# Handoff Document — 2026-03-23

## What Was Done This Session

### Strategic Review & Pivot
- Comprehensive strategic review of DIGIMON GraphRAG project
- Identified core problem: 50-tool surface overwhelms the routing agent, confounding the adaptive-routing thesis test
- Created ROADMAP with 5 phases (Plans #14-#18) to fix this
- Paused Plans #3-#6, #12-#13 (extraction quality, representation audit) pending thesis re-test results

### Literature Review (two rounds)
- Surveyed HippoRAG, HippoRAG 2, PropRAG, StepChain, EcphoryRAG, C2RAG, IRCoT
- Key findings: graphs help (+30 EM over vanilla RAG), passage-level nodes matter, PPR damping=0.5, question decomposition is #1 lever
- Artifact: `~/projects/investigations/digimon/2026-03-23-graphrag-sota-review.md`

### Plan #14: Fix llm_client — COMPLETE
- Benchmark runner works with gpt-5.4-mini
- llm_client imports clean in digimon conda env (was already fixed)

### Plan #15: Operator Consolidation — COMPLETE
- `Core/MCP/tool_consolidation.py` — 28 operators → 10 consolidated tools with method arguments
- `prompts/agent_benchmark_consolidated.yaml` — new prompt matching consolidated tool names
- Auto-selects consolidated prompt when `DIGIMON_CONSOLIDATED_TOOLS=1` (default)
- 9 unit tests passing (`tests/test_tool_consolidation.py`)
- **Behavioral validation**: agent now uses graph operators (relationship_search, entity_traverse, entity_info) instead of only chunk_text_search
- 10q comparison: consolidated (50% EM, $0.52) vs old surface (40% EM, $0.92)

### Plan #16: HippoRAG-Aligned Build Attributes — COMPLETE (code done, rebuild pending)
- Step 1: PPR damping 0.1→0.5 (`Config/RetrieverConfig.py`, `Core/Operators/entity/ppr.py`)
- Step 2: Decomposition via consolidated prompt (agent uses `reason(method="decompose")`)
- Step 3: Passage-level nodes (`enable_passage_nodes=True` in GraphConfig, `Core/Graph/BaseGraph.py`)
- Step 4: Co-occurrence-only mode (`skip_relationship_extraction=True`, `Core/Graph/ERGraph.py`)
- Entity VDB filters passage nodes (`Core/AgentTools/enhanced_entity_vdb_tools.py`)

### Plan #17: Thesis Re-Test — PLANNED (ready to execute)
- Detailed plan: `docs/plans/17_retest_thesis.md`
- Notebook planning artifact: `notebooks/plan17_thesis_retest.ipynb`
- Build config finalized (passage nodes, co-occurrence, synonym edges, PPR damping=0.5)

### Model Registry
- Added gpt-5.4-mini and gpt-5.4-nano to `~/projects/llm_client/llm_client/data/default_model_registry.json`
- gpt-5.4-mini is the recommended benchmark routing model

### llm_client Fix
- Reverted commit `80841d1` ("Delete relocated governance modules") which broke `build_gate_signals` import
- Revert commit: `3bac66e` on `digimon-stable` branch

## Current State

### Branch
`trip-backup-20260224_145348`, 46 commits ahead of remote. All changes committed, working tree clean.

### Pre-existing Codex modifications (unstaged from before this session)
These files were modified by the Codex agent's PTC work and are still dirty:
- `.claude/skills/digimon-operators/SKILL.md`
- `digimon_mcp_stdio_server.py`
- `docs/plans/progressive_disclosure_ptc_validation.md`

### Preliminary Benchmark Results (19q MuSiQue, pre-rebuild)

| Mode | LLM-judge | EM | Cost |
|------|-----------|----|------|
| Baseline (no graph) | 21.1% | 10.5% | $0.01 |
| GraphRAG old prompt (50 tools) | 36.8% | 31.6% | $1.19 |
| **GraphRAG consolidated (10 tools)** | **52.6%** | 42.1% | $0.67 |

**Graph wins**: 7 questions where baseline fails but GraphRAG succeeds
**Regressions**: 1
**Both fail**: 8 (iteration targets)

Result files:
- `results/MuSiQue_gpt-5-4-mini_baseline_20260323T171234Z.json`
- `results/MuSiQue_gpt-5-4-mini_20260323T163655Z.json` (old prompt)
- `results/MuSiQue_gpt-5-4-mini_consolidated_20260323T171612Z.json` (consolidated)

## What to Do Next

### Immediate: Plan #17 Phase 1 — Graph Rebuild

1. **Backup existing graph**:
   ```bash
   cp -r results/MuSiQue/er_graph results/MuSiQue/er_graph_pre_sota_backup
   ```

2. **Rebuild with SOTA config** (see `notebooks/plan17_thesis_retest.ipynb` Phase 1 for full config):
   - `enable_passage_nodes=True`
   - `enable_chunk_cooccurrence=True`
   - `schema_mode=open`
   - Extraction model: `gemini/gemini-2.5-flash`
   - Set config overrides and run `corpus_prepare → graph_build_er`

3. **Post-build enrichment**:
   - `augment_synonym_edges` (threshold=0.85)
   - `augment_centrality`

4. **Rebuild VDB**: `entity_vdb_build` (will auto-filter passage nodes)

5. **Smoke test**: 3q HotpotQAsmallest with rebuilt graph

### Then: Plan #17 Phase 2 — Compare

6. Re-run `eval/run_agent_benchmark.py --dataset MuSiQue --questions-file eval/fixtures/musique_19q_diagnostic_ids.txt --model openrouter/openai/gpt-5.4-mini --backend direct`

7. Cross-reference with baseline results (already collected)

8. For "both fail" questions, use `/iterate-failures` skill to diagnose

### Key Configuration

- `DIGIMON_CONSOLIDATED_TOOLS=1` (default) — uses consolidated 10-tool surface
- Consolidated prompt auto-selected when mode=hybrid + consolidated tools active
- PPR damping reads from `RetrieverConfig.damping` (default 0.5)
- Primary metric: **LLM-judge** (not EM)
- Routing model: `openrouter/openai/gpt-5.4-mini`

## Blockers & Dependencies

- **llm_client**: The revert of `80841d1` restored deleted governance modules. This is a band-aid — the benchmark runner imports `build_gate_signals` from llm_client which was deleted. Either update the import or keep the revert.
- **Graph rebuild cost**: ~$3-5 for MuSiQue extraction. Budget approved in Plan #17.
- **Passage nodes are untested on real data**: Code is implemented but no graph has been built with `enable_passage_nodes=True` yet. The rebuild is the first real test.

## Key Files Changed This Session

| File | Change |
|------|--------|
| `Core/MCP/tool_consolidation.py` | NEW — consolidated tool dispatch layer |
| `prompts/agent_benchmark_consolidated.yaml` | NEW — prompt for consolidated tools |
| `eval/run_agent_benchmark.py` | Modified — consolidated tools default, auto-prompt selection |
| `Config/GraphConfig.py` | Modified — added `enable_passage_nodes`, `skip_relationship_extraction` |
| `Config/RetrieverConfig.py` | Modified — `damping` 0.1→0.5, removed duplicate fields |
| `Core/Graph/BaseGraph.py` | Modified — passage node creation, co-occurrence auto-enable, HAS_PASSAGES capability |
| `Core/Graph/ERGraph.py` | Modified — skip_relationship_extraction gating |
| `Core/Operators/entity/ppr.py` | Modified — reads damping from config |
| `Core/AgentTools/enhanced_entity_vdb_tools.py` | Modified — filters passage nodes from VDB |
| `docs/plans/ROADMAP.md` | NEW — strategic roadmap with gates |
| `docs/plans/14-18_*.md` | NEW — five implementation plans |
| `docs/plans/CLAUDE.md` | Modified — plan index with active/paused/completed |
| `CLAUDE.md` | Modified — consolidated tool docs, literature review, routing model |
| `notebooks/plan17_thesis_retest.ipynb` | NEW — notebook planning artifact |
| `tests/test_tool_consolidation.py` | NEW — 9 unit tests |
| `~/projects/llm_client/.../default_model_registry.json` | Modified — gpt-5.4-mini, gpt-5.4-nano added |
| `~/projects/.claude/CLAUDE.md` | Modified — "Search before building" rule added |
